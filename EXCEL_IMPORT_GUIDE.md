# 菜谱 Excel 稳定导入教程

本教程适用于当前 `cute_food` MySQL 数据库。导入目标是五张业务表：

- `ingredients`：食材
- `recipes`：菜谱
- `recipe_ingredients`：菜谱和食材的关联
- `recipe_seasonings`：整道菜需要用到的调味料
- `recipe_steps`：菜谱步骤

不要直接把 Excel 导入 Navicat 的某一张表。一个菜谱涉及多张表，必须由后端统一校验后在同一个数据库事务中写入。

## 一、先使用空白模板

模板文件为 `outputs/excel-menu-import-template/今天吃什么呀-菜谱导入模板.xlsx`。

模板有七个工作表，其中有五张需要填写：

1. `食材`
2. `菜谱`
3. `菜谱食材`
4. `菜谱调味料`
5. `步骤`

`使用说明` 和 `字典` 不要删除，不要改名。`字典` 提供常用分类参考和菜谱难度的下拉选项；食材分类应以管理后台“分类管理”中维护的名称为准。

### 1. 食材

| 字段 | 含义 | 填写规则 |
| --- | --- | --- |
| 食材ID | 食材的永久编号 | 小写英文、数字、连字符；如 `pork-belly` |
| 食材名称 | 显示给用户的名称 | 如 `五花肉`；同一个名称只能出现一次 |
| 分类 | 用于页面筛选 | 填写管理后台已创建的名称，如 `肉类`。调味料不要再作为可选食材维护，请填到 `菜谱调味料` 工作表 |
| 图标 | 页面展示图标 | 一个或多个 emoji，如 `🥩` |

### 2. 菜谱

| 字段 | 含义 | 填写规则 |
| --- | --- | --- |
| 菜谱ID | 菜谱的永久编号 | 小写英文、数字、连字符；如 `pork-belly-rice` |
| 菜谱名称 | 显示名称 | 如 `五花肉焖饭`；同一文件内不能重复 |
| 简介 | 一句描述 | 不超过 255 个字符 |
| 用时(分钟) | 预计烹饪分钟数 | 正整数，如 `35` |
| 难度 | 展示难度 | 从下拉选项选择：`新手`、`简单`、`进阶` |
| 热量(kcal) | 每份热量 | 0 或正整数，如 `620` |
| 标签(用|分隔) | 页面筛选和展示标签 | 如 `家常|下饭|带饭`；使用英文竖线 `|` |

菜谱封面图标无需填写。后端会按 `菜谱食材` 工作表中同一道菜的“顺序”，读取对应食材的图标并自动合并，例如 `香菇`、`鸡肉`、`米饭` 会生成 `🍄🍗🍚`。

### 3. 菜谱食材

每行表示“一道菜需要一种食材”。同一道菜有三种食材，就要有三行。

| 字段 | 含义 | 填写规则 |
| --- | --- | --- |
| 菜谱ID | 对应 `菜谱` 工作表中的菜谱ID | 必须完全一致 |
| 食材ID | 对应 `食材` 工作表中的食材ID | 必须完全一致 |
| 顺序 | 食材在菜谱中的展示顺序 | 从 `1` 开始递增；同一菜谱不能重复 |

### 4. 菜谱调味料

每行表示“一道菜需要用到一种调味料”。调味料不再进入用户选食材页面，而是在菜谱详情和烹饪页面单独展示。

| 字段 | 含义 | 填写规则 |
| --- | --- | --- |
| 菜谱ID | 对应 `菜谱` 工作表中的菜谱ID | 必须完全一致 |
| 调味料名称 | 整道菜会用到的调味料 | 如 `食用油`、`盐`、`生抽` |
| 图标 | 页面展示图标 | 一个或多个 emoji，如 `🧂` |
| 顺序 | 调味料展示顺序 | 从 `1` 开始递增；同一菜谱不能重复 |

### 5. 步骤

每行表示“一道菜的一步”。同一道菜的步骤序号必须连续，从 `1` 开始。

| 字段 | 含义 | 填写规则 |
| --- | --- | --- |
| 菜谱ID | 对应 `菜谱` 工作表中的菜谱ID | 必须完全一致 |
| 步骤序号 | 该菜的步骤顺序 | 从 `1` 开始连续递增 |
| 做法 | 这一步的完整描述 | 不能为空 |

## 二、填写时必须遵守的规则

1. 不改工作表名称，不改第一行表头，不新增或删除列。
2. 一次只维护一个 Excel 文件，避免两份文件同时修改同一菜谱。
3. ID 创建后不要随意修改。改中文名称、简介、步骤都可以；改 ID 会导致关联数据和收藏记录失联。
4. `菜谱食材` 和 `步骤` 中引用的 ID，必须在同一个文件或数据库中已经存在。
5. 不要留下只有部分内容的半空行。程序会忽略整行空白，但会拒绝“只填了一半”的行。
6. 上传前保存为 `.xlsx`，不要另存为 `.csv`。CSV 无法保存多张工作表和下拉校验。

## 三、稳定导入不靠 AI 的工作方式

Excel 格式固定后，不需要 AI，也不需要每次找 Codex。后端可以完全按确定规则导入：

```text
上传 .xlsx
  -> 读取五张工作表
  -> 检查表名和表头
  -> 检查每一行的类型、必填项、ID 和重复项
  -> 检查菜谱与食材、调味料的关联是否成立
  -> 与 MySQL 现有数据比对，生成新增/更新/冲突预览
  -> 管理员确认
  -> 开启事务并写入五张业务表
  -> 成功后提交；任一步失败则全部回滚
```

这样即使某一行写错，也不会出现“食材已写入，但菜谱步骤没写入”的半成品数据。

## 四、已实现的后端接口

不要做“上传即写库”。推荐两个接口：

| 接口 | 作用 | 是否写数据库 |
| --- | --- | --- |
| `POST /api/admin/import/preview` | 上传并校验 Excel，返回预览和错误列表 | 否 |
| `POST /api/admin/import/commit` | 以预览令牌确认写入 | 是 |

### 预览接口应返回什么

```json
{
  "valid": true,
  "token": "临时导入令牌",
  "summary": {
    "ingredients": {"insert": 3, "update": 1, "skip": 12},
    "recipes": {"insert": 2, "update": 0, "skip": 8},
    "links": 7,
    "seasonings": 5,
    "steps": 9
  },
  "warnings": [],
  "errors": []
}
```

有任何 `errors` 时，`valid` 必须是 `false`，提交接口也必须拒绝该令牌。

## 五、后端导入器必须做的校验

### 1. 文件级校验

- 仅接受 `.xlsx`
- 限制文件大小，例如 5 MB
- 固定读取工作表：`食材`、`菜谱`、`菜谱食材`、`步骤`
- 每张表第一行必须和模板表头完全一致
- 拒绝含宏的 `.xlsm`

### 2. 单行校验

- 所有必填列不能为空
- ID 必须匹配正则：`^[a-z0-9-]{2,60}$`
- `用时(分钟)` 必须为大于 0 的整数
- `热量(kcal)` 必须为大于等于 0 的整数
- 简介不能超过数据库字段的 255 字符
- 标签拆分后不能有空标签

### 3. 文件内一致性校验

- 食材ID不能重复，食材名称也不能重复
- 菜谱ID不能重复
- 同一个 `(菜谱ID, 食材ID)` 组合不能重复
- 同一道菜的食材顺序不能重复
- 同一道菜的步骤序号不能重复，且必须是 `1, 2, 3 ...` 连续序号
- 每道新菜至少有一种食材和一个步骤

### 4. 与数据库的校验

- 不能用一个已存在的食材ID去写成另一个食材名称，除非在预览中明确标为“更新”
- 如果新菜谱引用的食材不在 Excel、也不在数据库，必须报错
- 更新已有菜谱前，预览必须告诉管理员该菜的食材和步骤会被整体替换
- 不允许删除正在被用户收藏或烹饪记录引用的菜谱；模板导入默认只新增和更新，不做删除

## 六、数据库写入顺序和事务规则

当前项目已经有 `connect()` 和 `seed_catalogue()`。Excel 导入器已复用 `connect()`，并在事务中执行以下顺序：

1. `connection.start_transaction()`
2. 批量插入或更新 `ingredients`
3. 批量插入或更新 `recipes`
4. 对本次“新增或更新”的每道菜，删除旧 `recipe_ingredients` 和旧 `recipe_steps`
5. 批量插入新的 `recipe_ingredients`
6. 批量插入新的 `recipe_steps`
7. `connection.commit()`
8. 任一 SQL 或校验失败：`connection.rollback()`

食材和菜谱建议使用如下策略：

```sql
INSERT INTO ingredients (id, name, category, icon)
VALUES (%s, %s, %s, %s)
ON DUPLICATE KEY UPDATE
  name = VALUES(name),
  category = VALUES(category),
  icon = VALUES(icon);
```

菜谱同理，但标签要先由 Python 转为 JSON 字符串，`art` 列则由本次菜谱关联食材的图标按顺序拼接生成。关联表和步骤表不要使用“追加”；对于本次覆盖的菜谱，应先删除旧关系再按 Excel 完整重建，避免旧食材或旧步骤残留。

## 七、建议的 Python 结构

后端安装一个额外依赖：

```powershell
python -m pip install openpyxl
```

并在 `requirements.txt` 添加：

```text
openpyxl>=3.1,<4.0
```

推荐新增模块：

```text
backend/
  excel_import.py        # 读取、规范化、校验、生成预览计划
  app.py                 # 上传、预览确认、管理员权限接口
uploads/                 # 临时文件，绝不公开访问
```

`excel_import.py` 至少拆成下面四个函数：

```python
def read_workbook(file_path) -> dict:
    """读取五张数据表，返回标准化的 Python 数据。"""

def validate_import(payload, connection) -> list[dict]:
    """只校验，不写数据库；返回带工作表和行号的错误。"""

def build_import_plan(payload, connection) -> dict:
    """区分 insert、update、skip，供前端预览。"""

def apply_import_plan(plan, connection) -> dict:
    """在一个事务中执行写入，失败时回滚。"""
```

错误信息必须带来源，例如：

```json
{
  "sheet": "菜谱食材",
  "row": 7,
  "field": "食材ID",
  "message": "食材ID 'basil' 不存在于食材表，也不存在于数据库"
}
```

这比只返回“导入失败”更容易改 Excel。

## 八、管理员每次导入的实际操作

1. 复制空白模板，文件名按日期命名，例如 `菜单-2026-08-03.xlsx`。
2. 按“食材 -> 菜谱 -> 菜谱食材 -> 步骤”的顺序填写。
3. 在 Excel 中用筛选或查找检查重复的 ID。
4. 保存并登录网站管理后台。
5. 点击“导入 Excel”，先执行预览。
6. 阅读新增、更新、跳过和错误统计。看到错误时，下载或查看错误清单，修改原 Excel 后重新预览。
7. 只有预览无错误且确认变更正确时，点击“确认写入数据库”。
8. 在 Navicat 中刷新 `cute_food`，检查相关表的行数和新菜谱的关联数据。
9. 打开网站检查新菜谱是否可见、食材筛选能否匹配、步骤是否按顺序显示。

## 九、上线前的安全措施

1. 每次确认写入前，自动导出一次备份，例如 `mysqldump cute_food`。
2. 仅管理员账户可访问预览和提交接口；当前项目已有 `@admin_required`，导入接口也必须加上它。
3. 上传的 Excel 文件使用随机文件名存到临时目录，导入完成后删除。
4. 预览令牌设 15 分钟过期，并绑定当前管理员账号与上传文件摘要，避免过期或换文件后误提交。
5. 在导入历史表中记录：操作者、文件名、时间、摘要、结果、错误数量。这个功能可以第二阶段添加。
6. 第一次使用时，先在测试库导入 1 个新食材和 1 个新菜谱，确认无误后再导入整批数据。

## 十、当前项目需要额外注意的点

后端已经提供 Excel 的预览和确认导入接口，入口位于 `http://localhost:3008/admin` 的“菜谱管理”页面。同时，后端也提供 `/api/ingredients` 和 `/api/recipes`；但当前 `index.html` 仍包含一份前端内置的食材和菜谱数组。

因此，Excel 成功写入 MySQL 后，前端页面不会自动展示新数据，除非把前端的读取逻辑切换为调用这两个 API。导入功能和“前端读取数据库”是两项应一起完成的改造：

```text
Excel 导入 -> MySQL
前端页面 -> Flask API -> MySQL
```

现在可以填写同一种模板并在管理后台上传；不需要 AI。若要让用户首页也立即显示数据库中新导入的菜谱，还需将前端数据源切换到现有 API。
