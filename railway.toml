[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m flask --app backend.app init-db && gunicorn backend.app:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/ping"
healthcheckTimeout = 120
